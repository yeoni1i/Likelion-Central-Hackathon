package com.example.atocuemobile.ui.screen.timeline

import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.Row
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.layout.fillMaxWidth
import androidx.compose.foundation.layout.padding
import androidx.compose.material3.Card
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp
import com.example.atocuemobile.ui.screen.timeline.component.EmptyMealCard

// TODO: 실제로는 등록된 사진/메뉴 데이터도 함께 받아서, 있으면 사진+메뉴 표시 / 없으면 EmptyMealCard 표시
@Composable
fun MealRecordTab(
    onAddRecordClick: () -> Unit
) {
    Column(modifier = Modifier.fillMaxSize().padding(16.dp)) {
        // 2x2 그리드 (아침/점심/저녁/간식)
        Row(modifier = Modifier.fillMaxWidth()) {
            EmptyMealCard(mealLabel = "아침식사", onClick = { /* TODO */ })
            EmptyMealCard(mealLabel = "점심식사", onClick = { /* TODO */ })
        }
        Row(modifier = Modifier.fillMaxWidth()) {
            EmptyMealCard(mealLabel = "저녁식사", onClick = { /* TODO */ })
            EmptyMealCard(mealLabel = "간식", onClick = { /* TODO */ })
        }

        Card(
            modifier = Modifier.fillMaxWidth().padding(top = 8.dp)
            // TODO: clickable { onAddRecordClick() } 추가
        ) {
            Text("+ 새로운 기록 추가", modifier = Modifier.padding(16.dp))
        }
    }
}