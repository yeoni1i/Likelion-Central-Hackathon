package com.example.atocuemobile.ui.screen.timeline

import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.Row
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.layout.fillMaxWidth
import androidx.compose.foundation.layout.padding
import androidx.compose.material3.Card
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

// TODO: 실제 값(샤워 횟수, 보습횟수, 증상 체크 여부, 특이사항 텍스트) 데이터 연동
@Composable
fun LifeRecordTab(
    onEditClick: () -> Unit = {}
) {
    Column(modifier = Modifier.fillMaxSize().padding(16.dp)) {
        Row(modifier = Modifier.fillMaxWidth()) {
            Text("샤워, 보습제", modifier = Modifier.weight(1f))
            Text("수정하기")  // TODO: clickable { onEditClick() }
        }

        Row(modifier = Modifier.fillMaxWidth()) {
            Column(modifier = Modifier.weight(1f)) {
                Text("샤워")
                Text("2회")  // TODO: 실제 값
            }
            Column(modifier = Modifier.weight(1f)) {
                Text("보습횟수")
                Text("20회")  // TODO: 실제 값
            }
        }

        Text("주요증상", modifier = Modifier.padding(top = 16.dp))
        Row(modifier = Modifier.fillMaxWidth()) {
            // TODO: 체크박스 형태 카드 2x2 (심한 가려움증 / 진물과 딱지 / 건조증 / ...)
            Card(modifier = Modifier.weight(1f).padding(4.dp)) {
                Text("심한 가려움증", modifier = Modifier.padding(12.dp))
            }
            Card(modifier = Modifier.weight(1f).padding(4.dp)) {
                Text("진물과 딱지", modifier = Modifier.padding(12.dp))
            }
        }
        Card(modifier = Modifier.padding(4.dp)) {
            Text("건조증", modifier = Modifier.padding(12.dp))
        }

        Text("특이사항 기록", modifier = Modifier.padding(top = 16.dp))
        Card {
            Text(
                "보습제를 다써서 급하게 기존과 다른 제품을 사용함. 제품은 00사의 0000",
                modifier = Modifier.padding(12.dp)
            )
        }
    }
}